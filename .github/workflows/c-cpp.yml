name: C/C++ CI

on:
  push:
    branches: [ "main", "feat/*", "fix/*" ]
  pull_request:
    branches: [ "main", "feat/*", "fix/*"  ]
  schedule:
  - cron: "0 3 * * 1"
  
jobs:
  build:
    name: make testing
    runs-on: ubuntu-latest
    steps:
    - name: Using Makefile to Build the project
      run: ../../make
      
      
      
